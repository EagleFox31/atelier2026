/** Bandeau fin aux couleurs kente (bleu, or, vert, terre cuite, ocre). */
export function LandingKenteBar({ className }: { className?: string }) {
  return <div className={`landing-kente-bar shrink-0 ${className ?? ''}`} aria-hidden />;
}
